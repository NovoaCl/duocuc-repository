Los indices son para mejorar la velocidad de las querys

```sql
SELECT
    *
FROM CREDITO C
    JOIN CREDITO_CLIENTE CC ON C.COD_CREDITO = CC.COD_CREDITO
;
```

```sql
SELECT
    MAX(COUNT(*)),
    nro_cliente
FROM producto_inversion_cliente pc
WHERE
    pc.cod_prod_inv IN (30, 35, 40, 45, 50)
GROUP BY
    nro_cliente
ORDER BY

```

```sql
CREATE INDEX idx_prod_inv_cli_max ON c.cod_
```
6 al 22 de julio = examenes
