### 24-04-2026

```sql
select * from cliente;

savepoint hola;

update cliente 
    set direccion = 'goicolea 554, San Ramón'
where
    numrun_cli = 13098962
;
rollback;
```

```sql
select * from cliente
where upper(direccion) like '%BALMACEDA%'
```

```sql
select numrun_cli from cliente
where upper(direccion) like '%BALMACEDA%
;

update cliente
    set direccion = 'goico'
where upper(direccion) like '%BALMACEDA%'
;
```

```sql
select
    sum(count(unique c.nurun_cli))
from cliente c
    left join arriendo_camion ac on c.numrun_cli = ac.numrun_cli
where ac.nro_patente is null
group by c.numrun_cli
;
```