```sql
DECLARE
    -- variables de la query

    -- variables del negocio
    v_comm          NUMBER(3) := 50;
    v_fecha         DATE;
    v_flag          BOOLEAN;
    v_nombre        VARCHAR2(50) := 'ALAN';

BEGIN
    -- Query + INTO

    --Lógica del negocio

    --Entregar el resultado
    v_comm := v_comm * 0.5;
    v_fecha := '01/05/2020';

    IF v_comm = 0 THEN
        v_flag := TRUE;
    END IF;

    DBMS_OUTPUT.put_line('Fin');
EXCEPTION WHEN OTHERS THEN
    DBMS_OUTPUT.put_line('Error');
END;
```