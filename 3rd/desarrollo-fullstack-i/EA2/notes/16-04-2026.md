### A Investigar

- Streams
- Hashmaps
- Funciones Lambda


### Persistencia

Querys + 1 modelo.

Fiesta >- Comuna

- Model si o si es el mismo que en DB
-
- Facturas: Folio, Rut, nombre, productos.

- Streams(): recorre cada elemento
- map(this::mapToDto): Toma cada fiesta y la transforma en FierstaResponse
- Collect(Collectors.toList()): convierte el stram de vuelta  a una lista.

- @RequestParam: pide el valor por url, api/v1/fiestas/capacidad?max=50

-properties
- Wallet ip, nombre puerto

- @Transactional: todos los metodos e esta clase deben ejecutarse dentro de una transaccion, si no la transaccion se revertirá(rollback) asegurando la consistencia de los datos.